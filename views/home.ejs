<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ruffin' Renown</title>
</head>
<body>
    <h1>Ruffin' Renown</h1>
    <% fidos.forEach(fido => { %>
        <div class="Dog">
            <%=fido.name %>
            <a href="/fidofame/<%=fido._id; %>">
                <img class="image" src=<%=fido.image %> alt=<%=fido.name %>>
            </a>
        </div>
        <% }) %>
<!-- 
        <div id='selector-flex'>

            <% let listOfDogs = [] %>
          
            <% Fidos.forEach(Fido(i) { %>
              <% if (!listOfDogs.includes(i.Fido)) { %>
                <form id="renown-form" action="/fidos/<%= i.fido.name %>" method="GET">
                  <button class='dog-selector' type='submit' value='<%= i.fido.name %>' name='renown' ><%= i.fido.name %></button>
                </form>
                <% listOfDogs.push(i.fido.name); %>
                <%console.log(listOfDogs)%>
              <% } %>
            <% }); %>
          
          </div> -->
</body>
</html>